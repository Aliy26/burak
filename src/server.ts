console.log("Activated!");

import moment from "moment";

const currentTime = moment().format("YYYY MM DD hh:mm");
console.log(currentTime);

const person: string = "Aliy";
const count: number = 100;
